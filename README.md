# AirMessage account site

A simple site that allows users to delete their AirMessage account.

## Usage

Set `firebase-config.json` from the Firebase console.

Run this command to build the site:

```shell
npm run build
```
